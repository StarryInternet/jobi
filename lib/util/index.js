'use strict';

const buildLogObject = require('./build-log-object');
const parseFormat    = require('./parse-format');
const parseLevel     = require('./parse-level');

module.exports = {
  buildLogObject,
  parseFormat,
  parseLevel
};
